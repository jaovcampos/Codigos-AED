//Atributos: Os atributos são as variáveis que armazenam dados dentro de uma classe. Eles representam as características
//ou propriedades dos objetos dessa classe. Por exemplo, em uma classe Carro, os atributos podem ser marca, modelo, ano, 
//cor, etc. Os atributos definem o estado de um objeto.

//Métodos: Os métodos são funções associadas a uma classe que representam seu comportamento. Eles podem ser usados para 
//modificar os atributos da classe, realizar cálculos ou interagir com outros objetos. Por exemplo, em uma classe Carro, 
//métodos podem ser ligar(), desligar(), acelerar(), frear(), etc. Os métodos definem o comportamento de um objeto.

 _____________________
|       Pessoa        |
|---------------------|
| - nome: String      |
| - idade: int        |
|---------------------|
| + cumprimentar()    |
| + envelhecer()      |
 ---------------------

//nome: Atributo privado (- indica visibilidade privada) do tipo String.
//idade: Atributo privado do tipo int.
//cumprimentar(): Método público (+ indica visibilidade pública) que não recebe parâmetros e retorna void.
//envelhecer(): Método público que não recebe parâmetros e retorna void.